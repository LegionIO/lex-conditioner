RSpec.describe Legion::Extensions::Conditioner do
  it 'has a version number' do
    expect(Legion::Extensions::Conditioner::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
